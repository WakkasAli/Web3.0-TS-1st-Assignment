var usernames = ["Admin", "manager", "supervisor", "Asst.Supervisor", "staff"];

usernames.forEach((user) => {
  console.log(`Greeting ${user}, You are logged in.`);
});


if (!usernames?.length > 0) console.log(`We need to find some users for Usernames Array List.`);

usernames = [];

if (!usernames?.length > 0) console.log(`We need to find some users for Usernames Array List.`);